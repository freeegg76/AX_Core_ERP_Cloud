# 원본 산출물 — 레포가 public 이므로 제외한다.
# 실제 SQL DDL·프로시저·트리거, DB/API 명세서, FR/UC 시트, 화면기획서는
# 내부 자산이므로 커밋하지 않는다. 설계서(AX_Bridge_시스템_설계서.md)만 관리한다.
Planning_Docs/

# 로컬 환경
.env
.env.local
node_modules/
.DS_Store
Thumbs.db
